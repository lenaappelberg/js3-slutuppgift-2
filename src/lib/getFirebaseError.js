export const getErrorMessage=(errorCode)=>{
    switch (errorCode) {
        case "auth/email-already in":
            
            break;
    
        default:
            return "Försök igen"
            break;
    }
}